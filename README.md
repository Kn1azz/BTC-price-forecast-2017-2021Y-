# 📈 BTC Time-Series Forecasting (Streamlit App)

Интерактивное приложение на Streamlit для анализа временного ряда цены Bitcoin и прогноза дневных лог-доходностей с использованием LightGBM и классических методов фичеинжиниринга.

Приложение позволяет загружать собственный CSV с историческими данными BTC, строить признаки, обучать модель, визуализировать прогнозы и сравнивать качество с бейзлайнами.

---

## 🚀 Основные возможности

### ✔️ Загрузка и обработка данных
- Поддержка формата `coin_Bitcoin.csv`
- Преобразование даты, сортировка и очистка
- Превращение цены в лог-доходности (стандартизированный метод стационаризации ряда)

### ✔️ Генерация признаков (Feature Engineering)
Включает:
- Лаги цены и лог-доходности  
- Rolling-признаки (mean / std)  
- Волатильность доходностей  
- EMA-фильтры  
- Календарные признаки (weekday / weekend)  

Все признаки сдвинуты с `shift(1)`, чтобы исключить утечку будущего.

### ✔️ Корректное разделение по времени
- Train / Valid / Test (по таймстемпам, без перемешивания)
- Настраиваемые пропорции

### ✔️ Моделирование
- **LightGBMRegressor**  
- Early Stopping  
- Возможность настраивать гиперпараметры из UI  
- Прогноз дневной доходности `target = log_return(t + horizon)`

### ✔️ Бейзлайны
- `return = 0` (модель «цена завтра = цена сегодня»)
- `return = ret_lag_1` (модель «как вчера»)

### ✔️ Визуализация
- **Actual vs Predicted Price** на тесте  
- **Actual vs Predicted Log-Returns**  
- Бар-чарт Feature Importance  
- Таблица фактов/прогнозов

---

## 📊 Научный вывод проекта
Дневные лог-доходности Bitcoin (t+1) ведут себя почти как шум:  
модель LightGBM **не превосходит** baseline «цена завтра = цена сегодня».

Это соответствует выводам современной финансовой литературы:  
крипторынки обладают сильной непредсказуемостью на коротком горизонте.

Приложение позволяет исследовать и подтверждать это поведение интерактивно.

---

## 🧪 Запуск проекта локально

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
